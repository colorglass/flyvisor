import <hwrng_inf.camkes>;

component Encrypt {
    include "crypto/ring_buffer.h";
    control;
    dataport Buf(4096) uart_reg;
    dataport ring_buffer_t recv_buf;
    consumes FC_data_ready fc_ready;

    uses hwrng_inf rng;
}